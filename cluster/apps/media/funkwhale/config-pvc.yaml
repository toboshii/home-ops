---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: funkwhale-config-v1
  namespace: media
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-block
